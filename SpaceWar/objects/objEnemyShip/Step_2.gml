/// @description Insert description here
// You can write your code in this editor
//x += velocityX;
//y += velocityY;

//THIS IS CODE FOR MOVING TOWARDS STAR
//dist = distance_to_object(objCenter);
/*
//get the distance
var cx = objCenter.x;
var cy = objCenter.y;

xdist = cx - x;
ydist = cy - y;
//point distance
crad = sqrt(sqr(xdist)+sqr(ydist));

//find length of circle
var clen = crad * pi * 2;

//get the point direction 
cdir = arctan2(ydist,xdist);
//find next angle on the circle
//var vel = sqrt((hsp * hsp)+ (vsp * vsp));
cnext = (cdir + 0.02) * 360 / clen;

//find coordinates of next point
var nextX = cx + crad * dcos(cnext);
var nextY = cy + crad * -dsin(cnext);

//speed
hsp += nextX -x;
vsp += nextY -y;


accel = 1/crad;
stardir = point_direction(x, y, objCenter.x, objCenter.y);


//motion_add(stardir, 2*accel);
hsp += 2*accel *cos(stardir);
vsp += 2*accel *sin(stardir);

//var Vel = 0.2;
//hsp += lengthdir_x(Vel,point_direction(x,y,objCenter.x,objCenter.y)-90);
//vsp += lengthdir_y(Vel,point_direction(x,y,objCenter.x,objCenter.y)-90);
*/